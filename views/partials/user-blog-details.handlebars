<div class="card">
  <div class="card-header">
    {{blogPost.date_created}}
  </div>
  <div class="card-body">
    <h5 class="card-title">{{blogPost.title}}</h5>
    <p class="card-text">{{short_desc blogPost.content}}</p>
    <a href="/blogPost/{{blogPost.id}}" class="btn btn-primary">Read more</a>
    <a href="/blogPost/{{blogPost.id}}" id="delete" class="btn btn-secondary">Delete blog post</a>
  </div>
</div>