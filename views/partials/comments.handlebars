<div class="card">
  <div class="card-body">
    <p class="card-text">{{comment_text}}</p>
    <p class="card-text" id="comment-createdBy">Left by {{user.username}} on {{format_date date_created}}</p>
  </div>
  <button id="deleteComment" data-id="{{id}}" class="btn btn-secondary">Delete comment</button>
</div>
<script src="/js/comments.js"></script>